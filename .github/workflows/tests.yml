name: tests

on:
  pull_request:
  push:
    branches: [main]

jobs:
  test:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - run: bash ops/tests/test_codex_health_check.sh
      - run: bash ops/tests/test_codex_cleanup.sh
